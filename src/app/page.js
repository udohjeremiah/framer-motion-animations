import clsx from "clsx";

export default function Home() {
  return (
    <main
      className={clsx(
        "flex min-h-screen max-w-[100vw] items-center justify-center p-4",
      )}
    >
      {/* Test your components here */}
    </main>
  );
}
